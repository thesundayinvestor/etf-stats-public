{
  "metrics": {
    /* ---------- Your factor scores (always read from meta) ---------- */
    "composite_score": { "display": "Composite Score", "format": "number", "visible": true },
    "quality_score":   { "display": "Quality Score",   "format": "number", "visible": true },
    "value_score":     { "display": "Value Score",     "format": "number", "visible": true },
    "growth_score":    { "display": "Growth Score",    "format": "number", "visible": true },
    "momentum_score":  { "display": "Momentum Score",  "format": "number", "visible": true },
    "risk_score":      { "display": "Risk Score",      "format": "number", "visible": true },
    "liquidity_score": { "display": "Liquidity Score", "format": "number", "visible": true },
    "size_score":      { "display": "Size Score",      "format": "number", "visible": true },
    "dividends_score": { "display": "Dividends Score", "format": "number", "visible": true },
    "sentiment_score": { "display": "Sentiment Score", "format": "number", "visible": true },
    "costs_score":     { "display": "Costs Score",     "format": "number", "visible": true },

    /* ---------- Example toggles (uncomment to hide entirely) ----------
    "asset_turnover_ttm": { "display": "Asset Turnover (TTM)", "format": "number", "visible": false },
    "price_ret_1m":       { "display": "Price Return (1M)",    "format": "percent", "visible": true }
    ------------------------------------------------------------------- */

    /* No other entries needed here; the app auto-discovers and adds the rest. */
    /* Note: any SA duplicates (value_score, growth_score, momentum_score, etc.)
       are remapped to *_raw with labels like "Value Score (SA)" so they don't
       collide with your factor scores above. */
    /* Phantom key guard: price_ret_3m is removed in code. */
    "_notes": "This map is override-only: it just sets labels/visibility for the few items that need control. All other numeric metrics from latest.json are discovered at runtime and shown by default."
  }
}